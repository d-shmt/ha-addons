name: "OpenCloud"
version: "1.0.0"     # <--- Das hier immer hochzählen bei Änderungen
slug: "opencloud"
description: "Sichere und schnelle Cloud-Lösung"
url: "https://github.com/opencloud-eu/opencloud"
arch:
  - aarch64
  - amd64
startup: application
boot: auto
ports:
  9200/tcp: 9200
map:
  - config:rw
  - share:rw
options:
  admin_password: "ChangeMe123!"
  oc_url: "https://homeassistant.local:9200"
schema:
  admin_password: password
  oc_url: str
# image: ...  <-- ENTFERNEN, falls vorhanden!
